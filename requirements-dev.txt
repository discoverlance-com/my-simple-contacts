# Development and testing dependencies
# Install production dependencies first: pip install -r requirements.txt
# Then install dev dependencies: pip install -r requirements-dev.txt

-r requirements.txt
pytest>=7.0.0
pytest-cov>=4.0.0